# AiSecurityScan 运行时依赖（与 pyproject.toml 保持一致）
typer>=0.12.0
rich>=13.0.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
PyYAML>=6.0
Jinja2>=3.1.0
httpx>=0.27.0

# 开发/测试
pytest>=8.0.0
mypy>=1.10.0

# LLM 增强（可选）
# openai>=1.30.0

# Web 接口 aiss serve（可选）
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9   # 控制台上传规则包需要
